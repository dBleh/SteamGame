#ifndef ENEMY_TYPES_H
#define ENEMY_TYPES_H

// Define the enemy types enum in a separate header to avoid circular dependencies
enum class EnemyType {
    Triangle,
    // Future enemy types...
    Circle,
    Square,
    Boss
};

#endif // ENEMY_TYPES_H