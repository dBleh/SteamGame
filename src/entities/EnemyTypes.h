#ifndef ENEMY_TYPES_H
#define ENEMY_TYPES_H

// Enum defining different enemy types for the manager
enum class EnemyType {
    Rectangle,  // Standard rectangular enemy (Enemy class)
    Triangle,   // Triangle enemy (TriangleEnemy class)
    // Add future enemy types here
};

#endif // ENEMY_TYPES_H